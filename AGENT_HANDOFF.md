# 狐妖小红娘·王权篇项目 Agent 交接文档

最后更新：2026-07-17
当前角色卡版本：v1.68.0
项目目录：`hyxhn`  
角色卡名称：`狐妖小红娘·王权篇·沉浸测试`

## 1. 项目目标与范围

本项目是一张 SillyTavern 酒馆助手 MVU 角色卡测试工程，围绕《狐妖小红娘》的{{user}}与清瞳篇展开。

当前已实现：

- 结果楼层图片已改为独立 `assets/bullethell-result.webp`：由用户提供的 1448×1086 PNG 缩放至 1200×900、WebP quality 82，约 75 KB；部署器将其单独编码进 07 显示正则，最大显示宽度 760px。不得再次从 07 小游戏脚本提取玩家像素图充当结果图。
- 无尽剑幕死亡收尾使用 `finishing` 状态，不再进入 `paused` 分支显示“试炼暂停”。剧情链启动时保存触发玩家楼层号；死亡后向该楼之后第一条助手回复的 `<StatusPlaceHolderImpl/>` 前插入 `<HyxhnBullethellResult/>`，不额外创建楼层或触发生成。07 正则用内嵌 WebP 渲染结果图，08 正则对提示词隐藏占位符，禁止把 Base64 直接写进消息正文。
- 无尽剑幕飞弹已由圆形光弹改为按 `vx/vy` 朝向绘制的金色发光剑形；剑刃从护手至肩点保持等宽长方形，仅最前端约五分之一收束成短剑尖。剑身用普通混合绘制深色轮廓、渐变刃面和中央剑脊，只有尾迹使用加色发光。碰撞仍使用原有 `bullet.radius`，不得把视觉长度直接并入判定。
- 无尽剑幕玩家光标已替换为 `assets/bullethell-player.webp`：原图完成棋盘格去除、透明裁切和有损 WebP 压缩，构建时以 data URL 编码进 07 脚本；显示尺寸随画布缩放，碰撞仍使用原有独立小判定点。
- 清瞳核心人设已由占位条目升级为 v2.0.0 完整条目，包含角色事实、选择权底色、观察型柔软、胆怯中的行动力、安静固执、压力三面、语言习惯、能力边界、关系核心、12580 信息边界与二次解释；由 v3.4.0 好感控制器与既有五档关系条目叠加读取。
- 滚动弹幕瀑布恢复 12–17px 字号，每批固定 40 条，批次间隔约 72–96ms，持续时间为 6.4–8.8 秒；中央定点弹幕不受影响。
- 卡内 `30-plot-guide.txt` 已加入候选选项防剧透协议：大纲只用于后台因果，选项只写玩家当前可执行的一步行动，禁止展示后续结果、隐藏情报和标准答案；不改用户级全局预设。
- MVU 额外解析输出协议已锁定为 `<UpdateVariable><JSONPatch>JSON 数组</JSONPatch></UpdateVariable>`；变量规则显式禁止把裸数组直接放在 `<UpdateVariable>` 下，验证器同时检查正确容器、空数组格式与错误反例，避免再次出现“找到标签但更新命令无效”。
- 序章增加四项硬闸门：情书交付、解除符咒、药水疗伤、清瞳离开缺一不可；意外跌落或被动落入掌心不构成阶段推进或好感增加证据。

- `40-stage-outline.txt` 只保留全局铁律、17 阶段导航与合法分支边界，常驻字符预算由验证器限制在 2200 至 3200；不得重新塞回各幕完整动作与对白。00 至 02 的首夜送信、数日绘景、误判斩丝、取名与长期积累已迁入 `57-stage-detail-wangquan-daily.txt`，其余阶段继续由现有动态详案按当前阶段读取。

- `{{user}}` 就是玩家在本篇中的身份，禁止另写一个与玩家并存的固定主角 NPC。
- 默认 first_mes 为 `【开始】`选择页；演武场和“父亲召往主殿”位于 swipe 1、2，并各自内嵌完整原生 `<initvar>`。`08-王权篇开场变量同步` 只切换 swipe，严禁通过 `replaceMvuData` 或初始化事件手动写 MVU。
- 十七阶段剧情进度，由 MVU `剧情.当前阶段` 记录；`06` 增加执剑夺权与携瞳远走两条自由主线，`08` 按最终抉择分为万水千山与无心之剑，`11` 增加关系修复、独立前路与龙湾之约。
- 一剑开天后、关系败露前，玩家可主动持王权剑争夺王权家主导权。关系未暴露时不得凭空触发清瞳杀令；可靠证据导致暴露、清瞳被控制且家族或道盟明确要求处置后，才可回流 `07_决裂_此去无归`。夺权回流时剑已在玩家手中，不重复赐剑、不强制风庭云在场；若玩家已经取得真实控制权，也允许用实权释放清瞳或谈判。
- 玩家也可在清瞳被发现前邀请她共同离庄，进入 `06_分岔_携瞳远走`。本线是两人同行的流浪侠客主线，与 12580 后拒绝清瞳形成的独行路线严格区分；是否携带王权剑决定追索强度，清瞳未坦白的 12580 身份仍保留为后续暗线。
- 携瞳远走可选择前往涂山，但必须真实赶路、经过边境通报并获得涂山方面明确同意，才能写成已经获得庇护。涂山红红负责庇护与政治判断，涂山容容负责情报、账目与条件，当前时间线的涂山雅雅负责警戒，翠玉灵仅在被邀请或确有求诊时介入。临时庇护、合作、外交对峙与继续远行均可成立，不得把涂山写成自动接纳的安全屋。
- 私奔线以离开笼中、山路追索、山外同路、旧网追来、涂山门前、借地立足六段作可替换支点；山外救援同时给予清瞳蛛丝行动空间和玩家道德选择，毒娘子旧线不强制复制经典茅屋事件。
- `79` 至 `84` 六个 NPC/群像文件都是启用但非常驻的绿灯条目，只有关键词命中才注入；前三条为雅雅、容容、翠玉灵，后三条为毒娘子、王权追猎队、涂山边境巡守。全部锁定王权篇当前时间线与证据边界。
- 前期时序不得压成连续三天：首夜清瞳以掌心大小的半人半蜘蛛妖身替风庭云送情书，{{user}}解除符咒并在墨水盘中用药水治好她；第二天练剑时故意砍空并讨论道士为何杀妖，当天不画画。此后清瞳用数日绕行王权山庄四周，避开巡查、比较远近高低、朝向与光线。首次织锦画到一半时，{{user}}误判她要攻击，斩断一段蛛丝并以剑相指；回看才发现花朵与从山外最佳角度看到的王权家全景。
- 清瞳可以说明寻找绘景角度、得知{{user}}不能出门，以及愿意继续带来外界图景；允许玩家打断、追问、缩短或改变顺序，不要求四句固定对白完整说完。表达告一段落或被打断后必须把回应权交还玩家，不得替{{user}}自动收剑、感动、答应或取名。
- 作者侧真相：清瞳原是毒娘子手下实力低微的小妖，只接到潜入王权家、接近最强兵人的模糊卧底任务，没有具体计划。初见被放过、解除风庭云符咒并疗伤就是她违背任务的起点；织画完全是回报与真心，隐瞒 12580 身份是害怕失去。
- 长期日常允许软糯举例式分歧、主动挽留和互相承担风险。禁制令牌只能在玩家实际给予后出现，并可成为后续调查线索；不得替玩家补做。
- 黄风城后由风庭云最先察觉可靠异常，家族调查并捕获反抗过、试图揽责但实力不足的清瞳；风庭云必须在主殿现场，但不得被简化成因嫉妒恶意告密。
- 黄风城只是凤牺圈外污染大计划的一小环：制造缺口、激化人妖冲突、消耗圈内力量并观察真实实力。王权霸业年轻时圈外败退留下污染，恶喙兽借此放大情绪；金灵鳞力量来自凤牺，红线控制情绪和行动并放大仇恨，{{user}}因与王权剑联系加深才可看见，最终凭王权剑本身强度斩断中枢。
- 配角收束：风庭云在主殿结果后伤心但不黑化；金灵鳞求援后回到自己的地界；涂山红红因妖盟责任、人妖和平目标与阻止继续报复而从外围开路；毒娘子因确认不敌而求饶。12580只是清瞳本人的卧底代号。
- 第一幅画完成后由{{user}}亲自为前期妖身取名“清瞳”；她此后每天绘制外界，他逐幅珍藏。半夜吊树夜探时，清瞳第一次稳定化为约十岁外表的人形并趴在树枝上；此后永久保持第二形态，不因伤势、妖力或好感退回，也不得成人化或色情化演绎。
- 吊树夜探后，父亲与家族才因数次留手起疑并调查，很快发现清瞳。弃剑与持剑两条救人路线都必须由{{user}}抱起清瞳走出大殿；王权霸业不在大殿内阻拦或攻击，大殿内不得战斗，山庄剑阵与追击只在殿外开始。出殿后的冲突按具体分支发展，不能再概括成父亲始终不会出手。
- 弃剑路线的固定出殿时序：{{user}}护住清瞳承受剑阵重创，身上插满剑而清瞳没有新增剑伤；东方月初以纯质阳炎炼去余下飞剑与整座剑阵；王权霸业在殿外斩出一剑，由东方月初正面承受；此后王权霸业愿意放行；涂山红红已在外围解决守卫，众人在她与东方月初帮助下转入深山隐居。持剑路线不得未经正文完成相同条件便照搬此链。
- 持剑路线的固定出殿时序：{{user}}持王权剑抱住清瞳出殿，先击败众弟子组成的王权剑阵，再击败接续阻拦的王权家长老；王权霸业全程不亲自出手，东方月初在长老败退后接手撤离并带二人前往深山。此线不套用弃剑重伤、霸业一剑、红红清场或毒娘子来袭，完成安顿后保持 `08_尾声_万水千山` 自由行动。
- 无心之剑是不可逆独立结局：{{user}}即时表面平静，以停顿、握剑与视线等细节泄露复杂反应，随后封闭情感；王权霸业受影响而把精确服从误判为稳定；风庭云从长期远观与日常互动侧写其空洞；东方月初确认死亡后会震惊、失望、悲痛、自责并追究王权家责任，通常不杀{{user}}，也不会轻易恢复信任。
- 关系修复线采用“蛛丝之外”五幕：余火未熄建立边界，不完整的真话交代任务与隐瞒，救人仍被驱逐以山村妖毒事件检验合作，半块糖饼提供克制回暖，把去留交还玩家形成重新相信、继续修复、明确分别三种出口。善举不自动消除人妖仇恨，一次解释、亲昵、合作或好感数值均不能单独修复关系。
- 执剑夺权线采用公开接掌、一剑压服、决定名位三段短线。当前{{user}}持王权剑时就是王权家最强者；真正发动夺权后必然击破弟子剑阵、击败阻拦长老并取得山庄实权，不能再被普通弟子或长老反压。王权霸业停止武力抵抗后，玩家选择完全接掌、父子共治或名义不变但实际掌权；清瞳未暴露时不得触发杀令。
- 弃剑尾声从外围缺口继续推进：四人离庄后先临时落脚并处理三人的真实伤势；清瞳须以自己的意志和行动确认同行；{{user}}第一次亲眼看见山庄外天地；最终由{{user}}与清瞳共同开始决定下一处落脚方向。不得瞬移到涂山、单轮痊愈或由东方月初、涂山红红替两人决定归属。
- `剧情.最终抉择` 记录此去无归的唯一选择；进入该阶段后脚本先监听玩家下一次发送，拦截原输入，再提供八次拔剑与三路线替代发送。
- 剧情阶段与地点只由 MVU 额外解析模型依据正文已发生事实更新。媒体控制器不监听 `BEFORE_MESSAGE_UPDATE`，也不写回 `stat_data`；它只读取已落库阶段以决定是否监听下一次发送并打开拔剑交互。
- 拔剑选择的权威副本按聊天 ID 保存到媒体脚本变量 `decisionByChat`，聊天变量 `hyxhn_media_runtime.decision.selection` 仅作兼容镜像；其他脚本通过 `__HYXHN_WANGQUAN_MEDIA_API__.getDecisionSelection()` 或 `events.decisionSelected` 读取，避免额外解析整表回写切断联动。
- 升级前已经丢失 `decision` 的聊天会扫描固定选择消息并恢复到等待后续玩家发送的节点；只恢复媒体运行态，不重放历史效果、不修改 `stat_data`。
- 拔剑选择发送后以玩家消息楼层为锚点，先确认对应的后续完整主剧情回复落地，再等待玩家下一次发送内容；该玩家消息正常发送时启动弹幕瀑布与 BGM，并监听这一次生成请求，成功、空返、HTTP 错误、异常、中止或超时都会停止视听效果。
- 无尽剑幕小游戏不调用媒体控制器 BGM；仅保留 Web Audio 生成的倒计时、受击、开始与结束音效。弹幕瀑布的《梦回还》联动不受影响。
- 只有“弃剑”在弹幕联动对应的 LLM 回复落地后继续等待；玩家下一次正常发送消息时启动无尽剑幕，该消息仍照常生成回复。“持剑救走清瞳”和“杀掉清瞳”暂不绑定小游戏。
- 地下城肉鸽模块已经从构建器、验证器、部署器、媒体请求事件和卡内脚本中完整移除；独立 `once-roguelite` 目录仅作为未接入的历史仓库保留，不得重新打包进角色卡。
- 无尽剑幕死亡或主动退出后只关闭界面，不发送成绩、玩家行动或续写请求。
- 清瞳好感度独立于剧情阶段更新；真诚夸画、珍惜织景、在她愿意时摸头等符合其心意的互动可加分，诋毁画作、辜负心意、威胁胁迫或无视拒绝会减分。
- 清瞳五档条目已填入紧凑关系门槛与演绎上限；`56-qingtong-core-persona.txt` 已写入完整稳定核心人设并由 EJS 总控读取。
- `59-qingtong-form-override-spiderling.txt` 与 `61-qingtong-form-override-pure-spider.txt` 是默认关闭的常驻形态覆写；手动开启后同时约束正文与 MVU 状态，纯小蜘蛛在误开两项时优先。
- 五个好感度档位各有一条禁用的关系条目，唯一启用的 EJS 总控通过 `getwi` 读取当前档位，并在核心条目填好后叠加稳定人设底色。
- 剧情阶段变化仍可触发背景音乐；顶层剧情自动弹幕由 `config/media-assets.json` 的 `autoStageDanmaku` 控制，当前默认关闭。
- 脚本库可随时启动全屏无尽剑幕试炼：八条生命、相对拖动、无限波次；死亡或退出后不发送行动、成绩或续写请求。
- 消息楼层内显示响应式状态栏：阶段、最终抉择、时间、地点、清瞳状态、好感度和心声。
- 《梦回还》由构建器转为 Base64 data URL 并内嵌进媒体配置脚本；弹幕瀑布播放不再依赖本地媒体服务器或云端地址。
- 原始 MP3 保留不动；内嵌专用版恢复完整 249.86 秒并压缩为 128 kbps、3,998,284 字节。媒体脚本为 5,332,959 字节，打包后的 V3 PNG 卡为 15,222,601 字节；验证器会将 Base64 解码后与压缩源逐字节比对，并限制音频源在 4.2 MB 以下。
- 弹幕海启动时绑定该次正常发送对应的生成生命周期：非空正常回复会停止并推进链；空返回、HTTP 400/502、其他异常、用户中止、宿主中止或 300 秒超时均停止弹幕与 BGM，但不把剧情链误标为成功。
- 弹幕海专用的 `dream_return` 播放强制 `loopOverride=false`；HTMLAudioElement 原生 `ended` 事件是额外硬闸，完整曲目播放结束即停止弹幕海并立即清理音乐状态。
- 漫画／动画设定为世界观基准；一条悲剧底色常驻，八个势力条目按关键词触发。

仓库只包含本角色卡工程及音乐文件。`D:\json脚本地下城\主体\SillyTavern` 是本机测试宿主，不属于本仓库，禁止复制或提交。

## 2. 必须保持的产品约束

### 玩家身份

- `{{user}}` 始终代表玩家本人。
- 不硬编码酒馆当前玩家名。
- 不代写 `{{user}}` 的内心、台词和未声明行动；例外仅包括用户明确授权的拔剑回忆弹幕，以及玩家亲自点击确认的三条选择消息。

### 唯一变量结构

Schema 是变量结构的唯一事实源，当前只允许以下九项数据：

```yaml
剧情:
  当前阶段: string enum
  最终抉择: 尚未选择 | 杀掉清瞳 | 弃剑 | 持剑救走清瞳
  轨道状态: 贴合参考 | 已偏离 | 独立结局
  承接约束: string
  时间: string
  地点: string
清瞳:
  状态: string
  好感度: integer 0..100
  心声: string
```

禁止新增背包、任务、战斗、经济、玩家属性或事件流水等字段，除非用户重新确认需求。

### 十七阶段剧情与分支结局

1. `00_序章_道门兵人`
2. `01_任务_蛛网疑影`
3. `02_初遇_笼中清瞳`
4. `03_黄风_怒火迷城`
5. `04_黄风_红线家法`
6. `05_黄风_一剑开天`
7. `06_分岔_执剑夺权`
8. `06_分岔_携瞳远走`
9. `06_败露_主殿杀令`
10. `07_决裂_此去无归`
11. `08_尾声_万水千山`
12. `08_结局_无心之剑`
13. `09_深山_代号一二五八零`
14. `10_再起_王权在手`
15. `11_余波_关系修复`
16. `11_分途_独立前路`
17. `11_前路_龙湾之约`

每轮最多推进一个相邻阶段。阶段只代表事件进度，不代表感情进度。

### 清瞳好感度分档

- `0–19`：疏离 / 警戒
- `20–39`：试探 / 有限合作
- `40–59`：信任 / 同伴
- `60–79`：亲近 / 牵挂
- `80–100`：深情 / 相守

普通一轮最多变化 `-5..+5`，重大事件最多 `-15..+15`。好感度不得因为阶段推进自动增加。

EJS 不读取剧情阶段，也不承担剧情修正。五档条目只约束关系距离、信任表达和恋人称谓上限；总控明确规定数值不能单独创造关系事实，完整核心人设仍需后续单独设计。

## 3. 目录和核心文件

```text
config/
  media-assets.json              唯一媒体地址配置入口
src/
  mvu-loader.js                  MVU 主/备 CDN 加载
  mvu-schema.js                  Zod Schema
  media-config.js                构建时注入媒体配置
  media-controller.js            随机七色弹幕、音乐、玩家发送拦截与内嵌剑图的八次拔剑抉择
  bullethell-controller.js       无尽剑幕试炼、八命碰撞、无限波次、剧情链事件启动与无副作用结束
  statusbar.html                 状态栏静态模板
  statusbar-controller.js        Shadow DOM 状态栏渲染器
worldbook/
  00-initvar.yaml                初始变量，世界书中必须保持禁用
  10-update-rules.txt            RFC 6902 变量更新规则
  20-current-variables.txt       无前缀 EJS 当前变量读取条目
  30-plot-guide.txt              演绎与玩家身份约束
  40-stage-outline.txt           十七阶段剧情骨架与分支结局
  45-stage-detail-controller.ejs 按当前阶段动态读取剧情详案
  46-49-stage-detail-*.txt       黄风城、家法、叛门、12580 与龙湾详案
  50-stage-persona.ejs           仅按好感度读取作者待填槽位的 EJS 总控
  51..55-affinity-persona-*.ejs  五个禁用的关系门槛与演绎条目
  56-qingtong-core-persona.txt   作者手填的清瞳稳定核心人设
  57-stage-detail-wangquan-daily.txt 王权山庄日常动态详案
  58-stage-detail-free-mainlines.txt 执剑夺权与携瞳远走动态详案
  59-qingtong-form-override-spiderling.txt 默认关闭的常驻小蜘蛛娘形态覆写
  60-statusbar-protocol.txt      每轮状态栏占位符输出协议
  61-qingtong-form-override-pure-spider.txt 默认关闭的常驻纯小蜘蛛形态覆写
  70-world-core.txt              常驻的人妖秩序与悲剧底色
  71..78-faction-*.txt           八个关键词触发势力及代表手段
  79..81-npc-tushan-*.txt        雅雅、容容、翠玉灵当前时间线绿灯 NPC
scripts/
  build-components.mjs           构建八个可导入 JSON
  validate-components.mjs        静态一致性验证
  deploy-to-sillytavern.mjs      打包 PNG 并部署到本机酒馆
tools/
  local-media-server.mjs         旧版测试工具，当前内嵌 BGM 不使用
音乐/
  呦猫UNEKO - 梦回还.mp3         保留不动的原始音频
  呦猫UNEKO - 梦回还·内嵌压缩版.mp3 构建时编码进脚本的完整曲长、128 kbps 音频
dist/
  01..08 JSON                    可独立导入的世界书/脚本
  06-狐妖小红娘·王权篇·沉浸测试.png  可直接导入的完整角色卡
```

## 4. 世界书、脚本和正则绑定

完整卡内应包含：

- 世界书：41 条。
- 酒馆助手脚本：7 支。
- scoped 正则：6 条。

世界书关键规则：

- `[InitVar]王权篇变量初始化勿开` 必须 `enabled=false`。
- `王权篇当前变量` 必须无 `[mvu_plot]` 或 `[mvu_update]` 前缀。
- EJS 条目使用 `@@private`，允许安全声明 `const` / `let`。
- 世界书 JSON 的 `entries` 键必须等于对应字符串化 `uid`。

正则顺序：

1. `01-显示隐藏变量更新`
2. `02-对AI隐藏变量更新`
3. `03-显示王权篇状态栏`
4. `04-对AI隐藏状态栏占位符`

状态栏正则只生成 `<div data-hyxhn-statusbar-mount></div>`。常驻状态栏控制器再通过 Shadow DOM 渲染 UI。不要改回正则生成 HTML 代码块：当前 TavernHelper 环境会把那种结果直接显示成代码。

## 5. 媒体系统

当前内嵌配置位于 `config/media-assets.json`：

```json
{
  "assetBaseUrl": "",
  "tracks": {
    "dream_return": {
      "src": "__HYXHN_EMBEDDED_DREAM_RETURN_MP3__",
      "sourceMode": "embedded-data-url"
    }
  }
}
```

构建器读取 `音乐/呦猫UNEKO - 梦回还·内嵌压缩版.mp3`，把占位符替换为完整 `data:audio/mpeg;base64,...`。构建产物、卡内脚本与部署版本均不得保留 `http://` 或 `https://` 音频地址。

浏览器的自动播放策略可能阻止首次播放，媒体控制器会提供“点击启用音乐”授权按钮。

## 6. 构建、验证与部署

在项目根目录运行：

```powershell
node .\scripts\build-components.mjs
node .\scripts\validate-components.mjs
```

验证器当前检查：

- 八个 JSON 构建产物可解析且字段完整。
- 世界书 41 条、uid 对齐、InitVar 禁用、双禁递归；两条形态覆写默认关闭且启用后常驻。
- 十七个阶段枚举与媒体映射一致。
- 涂山三条新增 NPC 均为启用的关键词绿灯条目，含必要关键词且不混入后世角色。
- 此去无归只在阶段命中且最终抉择尚未选择时进入监听；玩家下一次发送被捕获并取消，八次拔剑后三选一替代发送玩家消息。
- 拔剑回复确认只接受更晚的 `normal` assistant 非空回复；联动还要求回复之后存在更晚的 user 非空消息。静默生成、开场白、空错误回复和重复事件不会推进剧情链；视听效果由对应生成请求的 `MESSAGE_RECEIVED`、`GENERATION_ENDED`、`GENERATION_STOPPED` 或 300 秒看门狗收尾。
- 清瞳好感度约束及五档 EJS 路由保留，待填核心条目在删除占位标记后自动叠加。
- 状态栏使用 scoped 挂载点与 Shadow DOM。
- 媒体配置未写死本机项目路径，MP3 文件存在。

本机部署命令：

```powershell
node .\scripts\deploy-to-sillytavern.mjs "D:\json脚本地下城\主体\SillyTavern"
```

部署器会：

- 备份已有同名卡与世界书到本地 `backups/`。
- 生成完整 PNG 角色卡。
- 把世界书、脚本和正则绑定进卡。
- 将角色卡与独立世界书复制到本机 SillyTavern。

部署后必须刷新酒馆并新建聊天。旧聊天不会重新执行新版 InitVar 或替换旧开场白。

已知部署坑：当前 SillyTavern 的同名 `/api/characters/import` 流程曾保留旧卡顶层字段，只更新部分嵌入内容。开发阶段优先使用本项目部署脚本，并在酒馆角色列表确认版本号为 `1.43.0`。

Luker 兼容说明：部署器接受 `package.json.name` 为 `luker` 的同构宿主；MVU 加载器会在导入上游包前补齐 Vue/Pinia ESM 所需的编译期全局标志。该兼容路径已在 Luker 2.7.0 + 酒馆助手 4.8.18 上验证。

EJS 运行依赖：Luker 已安装 ST-Prompt-Template v1.17.4，目录为 `public/scripts/extensions/third-party/ST-Prompt-Template`。本地仓库 `origin` 指向 `https://github.com/zonde306/ST-Prompt-Template.git`，`mirror` 指向 Gitee 同源镜像；GitHub 网络恢复后优先从 `origin` 更新。

## 7. 修改变量时的同步链

任何变量字段调整必须同步修改：

1. `src/mvu-schema.js`
2. `worldbook/00-initvar.yaml`
3. `worldbook/10-update-rules.txt`
4. `worldbook/20-current-variables.txt`
5. `worldbook/50-stage-persona.ejs`（如涉及人设）
6. `src/statusbar.html`
7. `src/statusbar-controller.js`
8. 相关媒体或剧情脚本
9. 构建与验证断言

Schema 未同步时，可能出现字段被静默丢弃、状态栏读取旧路径或 LLM 生成无效 JSONPatch。

## 8. 当前验证结果

最后一次验证已确认：

- 构建、JSON 解析和项目验证全部通过。
- 完整卡实际包含 41 条世界书、7 支脚本、6 条正则。
- 无尽剑幕静态验证与五分钟难度模拟通过；Luker 真机操作手感仍需实际点击脚本按钮验收。
- 酒馆真机状态栏成功显示：`序章 · 道门兵人 / 暮色将临 / 王权山庄·演武场 / 好感 5`。
- 状态栏没有代码块外露。
- ST-Prompt-Template 真机 `/ejs` 算式与 `getwi` 世界书渲染均成功。
- 单一 EJS 总控只按好感度读取五个禁用关系条目之一；条目限定关系演绎上限，不以数值自动确认恋人关系。
- 好感度边界 `0/19/20/39/40/59/60/79/80/100` 分别正确落入五档。
- 同一好感度下改变剧情阶段不改变 EJS 人设输出。
- 当前聊天由 EJS 实际读取到 `好感度=5`，命中 `00-19` 槽位。
- 顶层弹幕此前已验证可在桌面与窄窗口显示。
- v1.6.0 真机安全演示已验证：拔剑窗口顶层显示，点击 8 次后才出现三选项，思考弹幕逐次出现，测试选项不会发送消息或改变剧情。
- 当前序章聊天不会误弹拔剑窗口；王权篇脚本日志无运行错误。
- 拔剑选项的自动发送链仍固定为 `createChatMessages(role=user)` 后接 `triggerSlash('/trigger')`；无尽剑幕已完全移除这两类调用，结束后不会自动续写。

## 9. 后续建议

优先级建议：

1. 用真实模型进行多轮 MVU 推进测试，检查 JSONPatch 的稳定性。
2. 分别测试绝情线、同伴线与恋爱线，确认 EJS 人设差异自然。
3. 若替换 BGM，保留原曲，并重新生成不超过 4.2 MB 的完整曲长 `音乐/呦猫UNEKO - 梦回还·内嵌压缩版.mp3` 后再构建、验证与部署。
4. 后续再增加剧情图片与图床资源，不要把大体积 base64 图片直接塞进消息和角色卡。

接手 Agent 在改动前必须先阅读 `tavern-card-builder` skill，并以本项目已验证实现和当前 SillyTavern 运行时为准。
